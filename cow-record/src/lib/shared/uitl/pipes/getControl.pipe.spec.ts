import { GetControlPipe } from './getControl.pipe';

describe('GetControlPipe', () => {
  it('create an instance', () => {
    const pipe = new GetControlPipe();
    expect(pipe).toBeTruthy();
  });
});
